import { getShopperContext } from '@/lib/sfcc/context'

export default async function () {
  // try {
  //   const context = await getShopperContext()
  //   console.log('context', context)
  // } catch (e) {
  //   // noop
  // }

  return null
}
